sudo apt install -y postgresql-client

psql --host=localhost --port=5432 --username=bob --db=mydb

\dn          # get all schemas
\dt          # get all relations
\d mytable   # get structure of mytable
\l           # get all databases
\c mydb      # connect to mydb